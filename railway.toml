[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python -m streamlit run dashboard_app.py --server.address 0.0.0.0 --server.port $PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
